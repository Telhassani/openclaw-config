
## Memory search fix — Gemini → Ollama local embeddings

- **Problem**: Gemini embeddings API key was leaked (403 error), memory_search completely broken
- **Solution**: Switched to local Ollama `nomic-embed-text` (137M, nomic-bert family)
- **Config**: `memorySearch.provider = "ollama"`, `model = "nomic-embed-text"`, `remote.baseUrl = "http://172.17.0.1:11434"`
- **Bonus**: Fixed Ollama provider `baseUrl` from `host.docker.internal` to `172.17.0.1` — also fixes heartbeat routing
- **Status**: Fully local, $0 cost, no external API dependency
- **Note**: Memory index will auto-reindex on restart (model/dimension change detected)

## Memory search test results

- Tested `memory_search` after switching from Gemini to Ollama
- Provider: `ollama` / `nomic-embed-text` ✅
- Ollama embeddings endpoint (`/api/embeddings`) returning vectors ✅
- Results empty on first call (index still building) — expected behavior
- No 403 errors, no permission denied

## Rule: Test after major modifications

- Always verify changes work immediately after applying them
- Don't assume; actually call the tool / endpoint / API and check the result
- Applies to config changes, code changes, system modifications

## ZENITH Phase 1 — Build Checkpoint (2026-04-03 22:55)

**Status:** Starting Phase 1 implementation from scratch in `/data/zenith_repo/`

**What exists:**
- `/data/zenith_repo/` — Clean git repo, pushed to GitHub (commit e76f78f)
- Contains: PLAN.md, CLAUDE.md, IMPLEMENTATION.md, README.md, PHASE1_IMPLEMENTATION_PLAN.md
- No source code yet — subagent output from April 2 was lost

**Phase 1 Plan (from PHASE1_IMPLEMENTATION_PLAN.md):**
1. Project scaffold (package.json, tsconfig, vite.config, tailwind.config, .env.example)
2. Backend: Express server + OpenClaw Gateway WebSocket (Protocol v3) + RPC wrapper + event relay + SQLite schema
3. Frontend: Vite + React 19 shell + Aurora background + glass panels + chrome bar + side rail + status bar
4. Zustand stores: gatewayStore, agentStore, uiStore
5. Hooks and utilities: useGateway, api.ts, utils.ts

**Critical files to create (15 total):**
```
server/: config.ts, index.ts, gateway/connection.ts, gateway/handshake.ts, gateway/rpc.ts, gateway/events.ts, ws/relay.ts, routes/system.ts, db/sqlite.ts, db/schema.sql, types/openclaw.ts
src/: main.tsx, App.tsx, router.tsx, styles/globals.css
src/components/layout/: AuroraBackground.tsx, Shell.tsx, ChromeBar.tsx, SideRail.tsx, StatusBar.tsx
src/stores/: gatewayStore.ts, agentStore.ts, uiStore.ts
src/hooks/: useGateway.ts
src/lib/: api.ts, utils.ts
Shared: package.json, tsconfig.json, vite.config.ts, tailwind.config.ts, .env.example, .gitignore
```

**Build order:**
1. Config files first (package.json, tsconfig, vite, tailwind, .env.example)
2. Backend scaffold (server/)
3. Frontend scaffold (src/)
4. Test: `bun run dev` should start both FE (:3000) and BE (:3001)

**Known constraints:**
- OpenClaw Gateway token-only auth grants ZERO scopes — must handle gracefully
- Backend should fall back to config file reads + CLI for agent data
- PORT env var is set to 55924 — backend must use 3001 explicitly
- SSH to Mac works: `ssh -i ~/.ssh/id_ed25519 tariq@100.82.80.22`
- GitHub push works via HTTPS fine-grained token

**If session dies mid-build:**
- Check `/data/zenith_repo/` for what files exist
- Resume from the next uncreated file in the build order
- The PHASE1_IMPLEMENTATION_PLAN.md has exact code for every file
## ZENITH Phase 1 — Complete Implementation (2026-04-03)

### Context Recovery
- Previous session had two subagents (`zenith-frontend-shell`, `zenith-backend`) that partially built ZENITH in `/data/zenith/` but their code was lost/not committed.
- Only architecture docs (PLAN.md, CLAUDE.md, IMPLEMENTATION.md) survived in `/data/zenith_repo/`.
- Lesson: Always commit incrementally and track working directory vs. repo separately.

### Access Configuration
- **VPS Container**: Direct `exec` access — Linux 6.8.0, x86_64, hostname `0eefeda55440`
- **Mac**: SSH via `ssh -i ~/.ssh/id_ed25519 tariq@100.82.80.22` — macOS 25.4.0, Apple Silicon (T6000), OpenClaw 2026.4.2
- Container SSH key (`openclaw-vps`, ed25519) added to Mac's `~/.ssh/authorized_keys` — SSH from container to Mac confirmed working
- Mac OpenClaw Gateway now running (fixed `gateway.mode=local` config issue, was blocking startup)
- Mac Gateway: `ws://127.0.0.1:18789`, RPC probe OK

### ZENITH Phase 1 Implementation
- **33 source files** created from scratch in `/data/zenith_repo/` following `PHASE1_IMPLEMENTATION_PLAN.md`
- Backend: Express on port 3001, OpenClaw Protocol v3 WebSocket (challenge-response handshake), RPC wrapper, event relay to browser clients, SQLite (WAL, 5 tables)
- Frontend: Vite + React 19 on port 3000, Aurora Cosmos background (3 animated orbs + noise), glassmorphic shell (ChromeBar 56px, SideRail 56px, StatusBar 36px), Framer Motion entrance animations
- Zustand stores: `gatewayStore` (WS connection), `agentStore` (fleet with role enum), `uiStore` (active panel, command palette)
- Config: `package.json` (Bun, React 19, Express, Zustand, Framer Motion, Tailwind 4), `vite.config.ts` (proxy to backend), `tailwind.config.ts` (aurora animations), `.env.example`
- Committed as `033317e` — pushed to Mac at `~/zenith_repo/`, GitHub push pending (needs fine-grained token configured on Mac's git remote)

### URLs
- `zenith.tariqvps.com` → Tailscale IP `100.101.104.37` — ZENITH not yet deployed (Phase 0 docs only)
- `pkos.tariqvps.com` → Live, PKOS dashboard (Phases 1-8 complete)
- `mcp.tariqvps.com/mcp` → Live, PKOS MCP server (21 tools)
- Port 55924 on container serves OpenClaw Control UI (not ZENITH)

### PKOS (Linked System)
- Personal Knowledge Operating System — Supabase (PostgreSQL + pgvector), 21 MCP tools
- Domain system: Obsidian, Learning, Comms, DermaAI, Homelab — custom dashboard views per domain
- Gemini embeddings (dashboard) + Ollama nomic-embed-text (MCP), 768d vectors, SHA-256 dedup
- Philosophy: Shared ontology, Resonance Engine, Pull-not-Push, Material Identity per domain

### Session Management Lesson (from previous failure)
- Subagents built code in `/data/zenith/` but repo was `/data/zenith_repo/` — directories diverged, code was orphaned
- Fix: Use single canonical repo directory (`/data/zenith_repo/`), commit after each file batch, verify files exist in both locations before proceeding
- Context preservation: Save checkpoints to `memory/YYYY-MM-DD.md` after major milestones
