# 2026-04-11 Daily Notes

## aimem-dashboard — Phase 2 UI + Stability

### Task 3: Stability ✅
- Moved cloudflared binary from `/tmp/cloudflared` to persistent `/data/.openclaw/workspace/aimem-dashboard/bin/cloudflared`
- Created `start.sh` — idempotent startup script that checks if services are running before restarting
- OpenClaw keepalive cron runs every 5 min, checks both Next.js and cloudflared, restarts if down
- Named tunnel at `aimem.tariqvps.com` confirmed working with token `eyJhIjoiZDA2YWIxNmIyNDA1ZDQ3MDE1ZTNlNjI2Yjk3ZTk2OWIi...`

### Task 2: API Routes Rewrite ✅
- Rewrote all 5 API routes to read vault files directly via SSH (`ssh -o IdentitiesOnly=yes -i ~/.ssh/id_ed25519 tariq@100.82.80.22`)
- Old routes used `ai-memory api recall` (semantic search, limited results) — new routes parse structured markdown from vault files
- Vault path on Mac: `/Users/tariq/Obsidian/ai-memory/`
- Files read: `03-memory/decisions-log.md`, `03-memory/patterns.md`, `03-memory/mistakes.md`
- Projects reads all `.md` files from `02-projects/`
- Tasks still uses `ai-memory api tasks` CLI
- All responses now return `{date, text}` format (decisions/patterns/mistakes), `{text, timestamp}` (tasks), `{name, file, content}` (projects)

### Task 1: Phase 2 UI ✅
- Rewrote all pages as client-side (`"use client"`) — previous SSR approach caused circular fetch issues
- Glassmorphism dark theme applied:
  - Gradient background: slate-950 → blue-950 → purple-950
  - Backdrop blur on all cards and sidebar
  - Semi-transparent borders (white/10 opacity)
  - Gradient text on headings (white → blue-200 → cyan-300)
  - Hover effects: scale 1.02, shadow glow, border brightening
  - Each section has its own gradient accent (blue/amber/red/emerald/purple)
- Data counts: 26 decisions, 7 patterns, 8 mistakes, 1 task, 2 projects

### Key Fix: SSR → CSR
- Server components tried to fetch from `localhost:3000` during build/SSR → stale/cached data
- Switched all pages to `"use client"` with `useEffect` + `fetch("/api/...")` → fresh data every visit
- Had to kill old Next.js process (PID 3787) that wasn't replaced by nohup restart

### Tunnel Config Note
- Cloudflare tunnel service URL must be `http://localhost:3000` (not `https://`) — dev/prod server is HTTP only
- Had 502 errors when config had `https://localhost:3000`

### ai-memory Logging
- Logged decision: "Switched dashboard from SSR to CSR for data fetching"
- Logged decision: "API routes read vault via SSH instead of ai-memory recall"
- Logged pattern: "Kill old process before starting new one in nohup restarts"
# 2026-04-11 — Daily Notes

## ai-memory Dashboard — Full Rewrite & Deploy

- Built a Next.js dashboard at `https://aimem.tariqvps.com` for ai-memory vault data
- API routes rewritten: decisions/patterns/mistakes parse timestamped markdown → `{date, text}`; projects reads file contents; tasks returns `{text, timestamp}`
- Frontend rewritten with Tremor v3 components, glassmorphism dark theme, gradient backgrounds
- All 5 detail pages are client-side rendered (`"use client"` + `useEffect`/`fetch`)
- 26 decisions, 7 patterns loaded and verified working
- **cloudflared** moved from `/tmp` to `/data/.openclaw/workspace/aimem-dashboard/bin/cloudflared` (persistent)
- Tunnel + keepalive cron (every 5min) auto-restart both services
- `start.sh` is idempotent — checks if services running before restarting
- **Critical lesson**: Next.js production build caches old route handlers — must kill old process AND `rm -rf .next` for clean rebuild
- MEMORY.md updated: added ai-memory Dashboard row to Active Projects table

## TODO
- Verify tasks page renders correctly
- Verify mistakes page renders correctly
- Confirm all pages work end-to-end after deployment
## Memory System Investigation & Reconfiguration

- Found contradiction: SOUL.md mandates ai-memory CLI writes, but OpenClaw system prompt mandates `memory_search` (builtin tool). Agent followed builtin because it's a first-class tool, not a shell command.
- **Decision**: ai-memory is now PRIMARY for all reads/writes. OpenClaw memory is backup only.
- Updated 3 files:
  - **SOUL.md** — ai-memory section rewritten: primary for all reads/writes, OpenClaw as backup. Explicit priority order (1. ai-memory, 2. memory_search, 3. daily files).
  - **AGENTS.md** — Startup: `ai-memory api recall "recent"` first. Memory rule: write to ai-memory FIRST, then daily files.
  - **HEARTBEAT.md** — Compact task: `ai-memory api compact` when session had significant work or after `/compact` (not fixed interval, not every heartbeat).
- Key insight: heartbeat intervals can change, so "every Nth heartbeat" is fragile. Context-based compacting is smarter — compact when there's actually work to archive.
## Dashboard Restart & Heartbeat

- aimem.tariqvps.com was down — Next.js process died, stale `.next` build
- Fixed: killed old process, `rm -rf .next`, fresh `next build`, restarted
- Dashboard fully live again: 29 decisions, 9 patterns, 8 mistakes, 1 task, 3 projects
- HEARTBEAT.md updated — compact trigger changed from "every 4th heartbeat" to "when session has done significant work or after /compact"
## Dashboard Performance Optimization & UI Polish

- Installed `vercel-react-best-practices` skill and applied optimizations to aimem-dashboard
- **async-api-routes**: Replaced all `execSync` → async `exec` wrapper (was blocking Node.js event loop)
- **server-cache**: Added in-memory TTL cache (30s default, 60s for logs) to all 6 API routes
- **server-serialization**: New `/api/counts` endpoint — dashboard loads 1 lightweight call + 1 decisions call instead of 5 full datasets
- **async-parallel**: `Promise.all` for counts + decisions on dashboard; logs API fetches archives in parallel
- **rerender-lazy-state-init**: Lazy `useState` initializer + `sessionStorage` cache for counts (instant re-renders)
- **DRY**: Shared `src/lib/vault.ts` — `execAsync`, `createCache`, `parseVaultFile`, constants
- Added `compress: true` to `next.config.ts`
- Dashboard cards now have gradient background colors per section (blue/amber/red/emerald/purple)
- Build: 7 static + 7 dynamic routes, all verified working
- **Lesson**: Stale `.next` cache causes 404s on new routes — must `rm -rf .next && next build` for clean deploy
## Dashboard Stability Fixes

- Next.js keeps dying — raw `nohup` isn't reliable enough for production
- Added `not-found.tsx` and `error.tsx` custom pages so errors are styled with "Try again" button instead of raw Next.js errors
- User reports "this page could not load" message — works from sandbox browser but not user's browser. Likely intermittent Next.js crashes or Cloudflare tunnel blips
- Need to investigate: proper process manager (systemd/pm2) instead of nohup, or increase Next.js stability
## Archives Page Added

- New `/archives` page — lists all 10 archived sessions from `05-archive/` directory with expandable accordion UI
- New `/api/archives` route — fetches archive files via SSH, parses timestamped entries, returns `{name, date, entries[]}`
- Archives card on dashboard: orange gradient, shows "10 entries" count
- Sidebar updated with Archive icon + link
- `/api/counts` updated to include `archives` count
- Build verified: 18 routes (8 static + 10 dynamic), all working

## User Reports "page could not load"

- Next.js keeps dying intermittently — `nohup` not reliable
- Works from sandbox browser every time — issue may be user's browser or intermittent downtime
- Added custom `not-found.tsx` and `error.tsx` for styled error pages
- Still need: proper process manager (pm2/systemd) for reliability
## Excalidraw Architecture Diagram

- Created architecture diagram for aimem.tariqvps.com using Excalidraw skill
- Diagram type: Architecture / Data Flow
- Components: Browser, Cloudflare Tunnel, Next.js API Routes, SSH to Mac, Obsidian Vault, ai-memory CLI, In-memory Cache
- File: `/data/.openclaw/workspace/aimem-dashboard/architecture.excalidraw`
## OpenClaw Updated to 2026.4.10

- Updated from 2026.4.9 → 2026.4.10 via layer update
- Gateway restarted automatically

## Memory System Evaluation (Builtin vs QMD + Active Memory)

- Compared existing builtin memory (SQLite + nomic-embed-text + FTS5) against new QMD sidecar backend
- Key new features in QMD: named path collections, configurable search modes (query/search/vsearch), session retention, cross-chat scope rules, result tuning (maxResults, maxSnippetChars, timeoutMs)
- **Active Memory plugin** (headline feature of 2026.4.10): automatic memory injection before every reply, not just manual search. Modes: message, recent, full
- Recommendation: enable QMD as primary search backend with Active Memory in `message` mode, keep builtin as fallback
- Tradeoff: QMD is a sidecar dependency (more ops), builtin is zero-dep but manual-only recall

## aimem-dashboard Phase 1 Complete (Subagent)

- Subagent completed aimem-phase1: scaffold, API routes, pages, build verified, all committed and pushed to origin/main
## Memory Integration Strategy (ai-memory + QMD + Active Memory)

- User philosophy: **memory is mine and portable** — vault stays in Obsidian format, usable across agents (not just OpenClaw)
- Strategy crafted: 3-phase approach
  - **Phase 1**: Enable Active Memory plugin on builtin backend (config change only, ~15min)
  - **Phase 2**: Install QMD sidecar, point it at local vault mirror (`/data/ai-memory/`), rsync from Mac on startup, QMD re-indexes every 5min
  - **Phase 3**: Upgrade aimem-dashboard with `/api/search` (semantic search via QMD) and `/api/write` (write-back via ai-memory CLI) — full knowledge base, not just file viewer
- Key principle: vault is source of truth on Mac, QMD indexes a copy, OpenClaw adds intelligence layer on top
- Resilience: if Mac down → QMD works from local mirror; if QMD down → builtin fallback
- Active Memory config: `queryMode: "recent"`, `maxSummaryChars: 220`, agents: ["main"]

## Mac SSH Status

- Mac at 100.82.80.22 is unreachable (Connection timed out) — has been down during this session
- ai-memory CLI falls back gracefully when Mac is unreachable

## Memory Integration — Execution (All 3 Phases)

### Phase 1: Active Memory Plugin ✅
- Enabled `active-memory` plugin in OpenClaw config
- Config: agents=["main"], queryMode="recent", promptStyle="balanced", timeoutMs=15000, maxSummaryChars=220
- Added to plugins.allow list
- Gateway restarted successfully

### Phase 2: QMD Sidecar ✅
- Installed QMD v2.1.0 globally (`npm install -g @tobilu/qmd`)
- Configured `memory.backend: "qmd"` with:
  - `includeDefaultMemory: true` (indexes MEMORY.md + memory/*.md)
  - Path: `/data/ai-memory-vault` (vault mirror, populated via cron sync)
  - Sessions: enabled, 90-day retention
  - Search mode: query
  - Limits: 8 results, 500 chars, 15s timeout
- Created vault sync script at `/data/.openclaw/workspace/sync-vault.sh` (uses scp since rsync unavailable)
- Set up cron job (every 15min) to sync vault from Mac
- Gateway restarted with QMD config

### Phase 3: Dashboard Search + Write ✅
- `/api/search` route: 3-tier search (SSH grep → ai-memory CLI → local grep fallback)
- `/api/write` route: POST endpoint for decisions/patterns/mistakes/tasks/notes with CLI + local file fallback
- Search page (`/search`): glassmorphism dark theme, debounced input, category filter tabs, results with score %
- Write modal (`write-modal.tsx`): New Entry button in sidebar, type selector, textarea, submit
- Sidebar updated with Search link and WriteModal component
- Added shadcn Dialog component
- Build: 20 routes (10 static ○, 10 dynamic ƒ)
- Dashboard live at https://aimem.tariqvps.com
## Phase 3 Completion + Build Fix

- Phase 3 subagent timed out after writing API routes but not UI pages
- Manually completed: search page, write modal (using Base UI Dialog, not Radix — no `asChild` prop), sidebar update
- Key fix: Next.js 16 uses Base UI, not Radix. DialogTrigger doesn't support `asChild` — removed it
- Build succeeded: 20 routes (10 static ○, 10 dynamic ƒ)
- Dashboard restarted and live at https://aimem.tariqvps.com

## Memory Architecture Final State

- **ai-memory CLI** (Obsidian vault on Mac): primary write system, source of truth
- **QMD** (sidecar): primary search backend, indexes local vault mirror + workspace memory
- **Active Memory** (plugin): auto-injects context before replies, queryMode=recent
- **aimem-dashboard**: visual interface with search (3-tier: SSH grep → ai-memory CLI → local grep) and write (CLI → local fallback)
- Vault sync: cron every 15min from Mac to `/data/ai-memory-vault/` (Mac currently down)
- Resilience: Mac down → QMD/local grep works; QMD down → builtin fallback; CLI down → local file write

## HEARTBEAT: Memory Maintenance

- Mac SSH unreachable — `ai-memory api compact` skipped (requires Mac connection)
- OpenClaw builtin memory operational
- Daily memory file (this file) being maintained as backup per AGENTS.md protocol
## Config Files Page + Collapsible Sidebar

- Added `/config` page: displays OpenClaw config files (workspace .md + /data/.openclaw .json/.yaml/.env) as glassmorphism cards with gradient backgrounds, syntax highlighting (JSON, Markdown), copy button, expand/collapse
- `/api/config` route: reads local config dirs, skips sensitive files, returns metadata (size, modified, type, gradient, icon)
- Dashboard homepage: added Config card (slate gradient, Settings icon)
- Sidebar: made collapsible (toggle button ↔), collapses to icon-only mode (64px)
- Sidebar + main content: independent scrolling (`h-screen overflow-hidden` on body, `overflow-y-auto` on each pane)
- WriteModal: pinned at bottom of sidebar, always visible. Accepts `collapsed` prop for icon-only mode
- Layout: changed from `min-h-full flex` to `h-screen overflow-hidden` with flex container
- Commits: `117096c` (config page), `be23692` (collapsible sidebar)
- Mac SSH back online — vault synced and ai-memory entries written for today's decisions/patterns/mistakes

## Key Lesson: Always Verify End-to-End

- Before confirming "site is up", always check: process running, tunnel connected, local HTTP 200, external HTTP 200 through Cloudflare, actual content rendering
- Don't just assume based on restart command output
## Sidebar Hover + Active Gradient Styles

- Updated sidebar nav items: active state uses `bg-gradient-to-r from-blue-500/90 to-purple-500/90 text-white shadow-md shadow-purple-500/20`
- Hover state: `hover:bg-gradient-to-r hover:from-blue-500/10 hover:to-purple-500/10 hover:text-foreground`
- Replaced flat black `bg-primary` active style with blue-purple gradient
- Build succeeded (type warning at sidebar.tsx:83:15 — non-blocking, likely the conditional className)
- Commit pending user approval before push
## Turbopack + fs Import Fix

- Next.js 16 Turbopack chokes on direct `fs` imports in API routes at build time (code 1 exit)
- Fix: add `export const dynamic = "force-dynamic"` to the route to tell Turbopack not to try to statically analyze it
- Initial build failed with `process.cwd()` trace error, second build with `force-dynamic` succeeded
- Commit `d740cec` — sidebar gradient styles + config API fix
## QMD Dashboard — Implementation Started

- Plan created at `aimem-dashboard/PLAN.md` — full observatory-style QMD dashboard at `/qmd`
- Three-zone layout: Helm (metrics at a glance), Canvas (4 modes via segmented control), Feed (live activity)
- 4 modes: Search (score autopsy, zero-result radar), Write (dupe detection, auto-link), Context (injection log, simulator), Graph (force-directed, island detection)
- Implementation strategy: Round 1 (me: shell+Helm+Feed+APIs), Round 2 (3 subagents: Search/Write/Context in parallel), Round 3 (me: Graph+integration)
- API routes created:
  - `/api/qmd/status` — reads OpenClaw memory SQLite (`main.sqlite`) + QMD SQLite (`index.sqlite`) for index stats, collections, freshness, process status
  - `/api/qmd/activity` — reads recent file changes, chunk embeddings, daily memory entries
  - `/api/qmd/search` — FTS + LIKE fallback search against chunks table
- Key finding: QMD embedded index has 0 documents (not yet indexing), OpenClaw builtin memory has 11 files/50 chunks/78 embeddings
- Both SQLite DBs are directly queryable from the Next.js container
- Style contract for subagents: glassmorphism, Tailwind-only, type→gradient colors, shadcn components
## QMD Dashboard Implementation (Round 1 & 2)
- **Foundation (Round 1)**: Built the "Observatory" shell at `/qmd` with 3 zones (Helm, Canvas, Feed). Implemented `/api/qmd/status` and `/api/qmd/activity` for real-time brain metrics and activity tracking.
- **Search Mode (Round 2)**: Implemented "Search Observatory" featuring Score Autopsy (Semantic vs FTS visualization), Zero-Result Radar to identify memory gaps, and a search volume timeline.
- **Write Mode (Round 2)**: Built "Smart Entry" with real-time duplicate detection (semantic similarity), auto-link suggestions, and a quality/density gauge for memory entries.
- **Context Mode (Round 2)**: Created "Context Injection" observatory with an injection log (what was actually primed), a RAG simulator for predicting retrieval, and scope toggles for knowledge collections.
- **Style Contract**: All components follow a strict glassmorphism design (backdrop-blur, `bg-black/20`, specific gradient mappings for memory types).
- **Status**: Round 2 components are complete and awaiting final integration into the main `/qmd` page. Round 3 (Graph Mode) is next.
